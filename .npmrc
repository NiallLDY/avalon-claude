# workspace 内部依赖一律走 link，不从 registry 拉
link-workspace-packages=true
prefer-workspace-packages=true
# 严格 peer 依赖，避免幽灵依赖
strict-peer-dependencies=false
auto-install-peers=true
# 容器内 store 放在具名 volume 里，见 compose.dev.yaml
